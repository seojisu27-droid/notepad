# 로컬 개발용 환경변수 예시입니다.
# 이 파일을 복사해서 .env 로 만들고 값을 채우세요.  cp .env.example .env

# PostgreSQL 연결 문자열
# 예: postgres://user:password@localhost:5432/notepad
# Render 배포 시에는 render.yaml 이 DATABASE_URL 을 자동 주입하므로 설정하지 않아도 됩니다.
DATABASE_URL=

# 서버 포트 (로컬 기본값 3001). Render 에서는 자동으로 주입됩니다.
PORT=3001
