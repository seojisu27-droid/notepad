# 로컬 개발용 환경변수 예시입니다.
# 이 파일을 복사해서 .env 로 만들고 값을 채우세요.  cp .env.example .env

# Turso(libSQL) 연결 정보
# turso db show <db이름> 에서 URL 확인, turso db tokens create <db이름> 으로 토큰 발급
TURSO_URL=libsql://your-db.turso.io
TURSO_TOKEN=

# 서버 포트 (로컬 기본값 3001). Render 에서는 자동으로 주입됩니다.
PORT=3001
